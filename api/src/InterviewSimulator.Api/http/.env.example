# Copy this file to .env and fill in your values. The .env file is gitignored.
# Get AUTH_COOKIE from DevTools → Application → Cookies after logging in via /api/auth/login.
# Include the full cookie name=value pair.
AUTH_COOKIE=InterviewSimulator.Auth=<paste-cookie-value-here>
